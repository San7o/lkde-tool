# This is the default environment. You can specify the variables here, they
# will be imported into the toolchain.
#
# Or you could also create a new environment and use it:
#
#     touch .env-linux-aarch64 && export ENV=linux-aarch64
#