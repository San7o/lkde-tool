# Do not build dependencies
# This assumes you have all build dependencies already installed

BUILD_DEPS=false
DEPS_INSTALL_DIR=/usr
KERNEL_FLAGS=ARCH=${TARGET_ARCH} \
                  CROSS_COMPILE=${DEPS_INSTALL_DIR}/bin/
